1. Study about the specifics on depends_on and condition fields in a docker-compose file, and learn how to enforce the backend to either not even start or completely halt if the services it's dependent on encounter a problem while or after starting. 

2. Learn about single thread concurrency, asynchronous programming (asyncio in Python), event loops, blocking vs. non-blocking I/O.